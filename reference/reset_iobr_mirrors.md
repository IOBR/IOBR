# Reset Download Mirrors to Default

Resets the download mirror list to the default values.

## Usage

``` r
reset_iobr_mirrors()
```

## Value

Invisibly returns the default mirror list.

## Examples

``` r
reset_iobr_mirrors()
#> ✔ Download mirrors reset to default
```
